// import { useQuery } from '@tanstack/react-query';
import { UserService } from '@/api/user';

export class UserQueries {
  // static useUser(userId: string) {
  //   return useQuery({
  //     queryKey: ['user', userId],
  //     queryFn: () => UserService.fetchUser(userId),
  //     enabled: !!userId,
  //   });
  // }
}
