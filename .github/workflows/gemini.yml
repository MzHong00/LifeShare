name: Gemini Code Review

on:
  pull_request:
    types: [opened] # synchronize를 제거해서 푸시할 때마다 실행되는 걸 막습니다.
  issue_comment:
    types: [created]

jobs:
  gemini:
    # PR이거나, PR에 달린 댓글일 때만 실행 (일반 Issue 댓글 제외)
    if: github.event.pull_request || (github.event.issue.pull_request && github.event.comment)
    runs-on: ubuntu-latest
    steps:
      - name: 📦 체크아웃
        uses: actions/checkout@v4

      - name: 🤖 Gemini 실행
        uses: google-github-actions/run-gemini-cli@v0.1.20
        with:
          gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
          prompt: 'GEMINI.md 파일과 프로젝트의 전반적인 코드 스타일 패턴 등을 참고해서 PR 내용의 개선점이나 버그 위험을 한국어로 리뷰해줘.'
